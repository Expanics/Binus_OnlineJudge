#include <stdio.h>
#include <math.h>

int main() {
    // double X, Y, akhir;

    // int a, b, c;
    // scanf("%d %d %d",&a,&b,&c);
    // printf("%.2lf\n", ((double) a * 0.2) + ((double) b * 0.3) + ((double) c * 0.5));


    int a, b, c, d, e, f, g, h;
    scanf("%d %d",&a,&b);
    scanf("%d %d",&c,&d);
    scanf("%d %d",&e,&f);
    scanf("%d %d",&g,&h);
    printf("%d\n", a * b);
    printf("%d\n", c * d);
    printf("%d\n", e * f);
    printf("%d\n", g * h);
    
    return 0;
}